# 无人机集群 REID 轨迹检索项目说明

更新日期：2026-06-24

## 1. 项目目标

本项目目标是构建一个面向无人机场景的 **人物 REID 轨迹检索系统**：无人机在场地内悬停或巡航拍摄，目标检测与局部跟踪模块持续处理画面中的人物，保存原始 RGB 图像、深度图像、人物裁剪图、人物像素框、时间戳和局部跟踪 ID 等观测信息；当用户上传一张查询人物图像时，系统通过 REID 判断该人物是否在历史观测中出现过，并在命中时从完整历史观测中恢复其空间坐标与运动轨迹，最终显示该人物的历史轨迹。

当前阶段先聚焦于 **历史回放流程验证**，不优先做复杂的实时接入、实时关注和多无人机分布式同步。首版目标是拉通“历史数据回放 → 摘要数据库构建 → 查询图 REID → 完整数据库检索 → 轨迹生成 → 历史轨迹显示”的完整流程。

后续阶段可以在历史回放闭环稳定后，再扩展到实时检测入库、查询未命中后的持续关注、多无人机分布式数据库和实时 BEV 展示。

## 2. 核心术语

- **REID**：人物重识别。输入人物图像，计算 embedding，并与历史 embedding 计算相似度，用于判断是否为同一全局人物。
- **embedding**：人物图像经过 REID 模型编码得到的特征向量。
- **相似度阈值**：判断 REID 是否命中的最低相似度要求。超过阈值的候选中取相似度最高的 top1 作为识别结果。
- **局部人物 ID / local track ID**：目标检测与局部跟踪模块在单个无人机、单段视频或单个视野内分配的人物 ID。该 ID 只表示本地连续跟踪结果，不代表历史全局身份。
- **全局人物 ID / global person ID**：系统层面维护的人物身份 ID，由摘要数据库中的 REID 结果决定，用于跨时间、跨视野、跨无人机合并同一人物的观测。
- **摘要数据库 / 人物库**：保存所有入镜过人物的代表图像、embedding、来源无人机、局部 ID 与全局人物 ID 等轻量信息。当前设计中不单独维护“人物库”，摘要数据库本身就是人物库，因为每个新入镜的人都会上传一张代表人物图像到摘要数据库。
- **完整数据库**：保存持续观测过程中的完整图像、深度图、人物裁剪图、像素框、时间戳、无人机位姿等信息，主要服务于轨迹恢复和历史回放。
- **BEV**：鸟瞰图。系统初始阶段由无人机拍摄俯瞰图并拼接生成，用于展示人物轨迹和当前位置。首版重点是显示历史轨迹，后续再扩展实时位置刷新。

## 3. 当前首版范围：历史回放

当前阶段不把实时接入作为首要目标，而是先基于已有或离线回放数据验证完整链路。

### 3.1 历史数据来源

历史回放数据可以来自真实无人机采集，也可以来自离线数据集或提前保存的实验数据。数据中应包含：

- 原始 RGB 图像序列；
- 深度图像序列；
- 时间戳；
- 人物检测框；
- 人物裁剪图；
- 局部人物 ID；
- 相机参数、无人机位姿或其他可用于坐标转换的辅助信息。

在真实运行场景中，这些数据由目标检测与局部跟踪模块在无人机飞行过程中持续产生并写入完整数据库；但首版可以先用历史数据模拟这个结果。

### 3.2 摘要数据库构建流程

1. 遍历历史回放数据中的局部人物轨迹。
2. 对每个新出现的局部人物 ID，选取一张代表人物图像。
3. 对代表人物图像计算 embedding。
4. 将代表人物图像、embedding、局部 ID、来源无人机、首次出现时间等信息写入摘要数据库。
5. 通过 REID 判断该局部人物是否对应已有全局人物 ID：
   - 若命中已有全局人物，则关联到已有全局人物 ID；
   - 若未命中，则在摘要数据库中新增一个全局人物 ID。
6. 完整数据库中的该局部人物后续观测记录应关联到对应的全局人物 ID。

摘要数据库因此承担人物库功能：只要某个人在历史数据中入镜过，理论上就应该至少有一张代表图像及其 embedding 存在于摘要数据库中。

### 3.3 查询人物流程

1. 用户上传一张查询人物图像。
2. 系统计算查询图像的 embedding。
3. 系统只在摘要数据库中比较查询 embedding 与历史代表人物 embedding 的相似度。
4. 对所有超过阈值的候选，选择相似度最高的 top1 作为 REID 结果。
5. 若没有任何候选超过阈值，则认为该人物没有在历史摘要数据库中出现，本次查询失败。
6. 若命中已有全局人物 ID，则到完整数据库中查询该全局人物 ID 的所有历史观测。
7. 轨迹生成模块根据历史观测中的原始 RGB 图、深度图、人物像素框、相机参数和无人机位姿，计算人物在各时刻的空间坐标。
8. 系统将连续坐标连接成历史轨迹，并显示该人物的历史轨迹。

当前首版中，查询未命中时不新增人物，也不创建实时关注任务。后续如果接入实时查询与持续关注能力，可以再把未命中的查询人物注册到摘要数据库或关注任务中，用于后续新观测到来时继续匹配。

## 4. 总体业务流程

### 4.1 历史回放闭环

1. 准备历史完整观测数据。
2. 由历史观测数据构建完整数据库。
3. 从每个新出现的局部人物 ID 中抽取代表人物图像。
4. 对代表人物图像计算 embedding，并写入摘要数据库。
5. 摘要数据库维护全局人物 ID，承担人物库功能。
6. 用户上传查询人物图像。
7. REID 模块在摘要数据库中完成相似度检索。
8. 查询命中时，根据全局人物 ID 到完整数据库中拉取所有历史观测。
9. 轨迹生成模块将历史观测转换成轨迹点序列。
10. 显示模块展示该人物的历史轨迹。

### 4.2 后续实时闭环方向

实时能力暂不作为首版重点，但整体架构应保留扩展空间。

后续实时流程可以是：

1. 无人机飞行后，目标检测与局部跟踪模块持续运行。
2. 模块持续采集原始 RGB 图像和深度图像，记录时间戳，并写入完整数据库。
3. 检测到人物后，保存人物裁剪图、人物像素框和局部人物 ID。
4. 每当出现新的局部人物 ID，将第一张代表人物图像上传到摘要数据库。
5. 摘要数据库通过 REID 为该局部人物分配已有或新的全局人物 ID。
6. 查询任务可以由地面站发送给任意无人机。
7. 接收查询任务的无人机查询摘要数据库，完成 REID 检索。
8. 查询命中后，系统从完整数据库中获取该全局人物 ID 的历史观测并生成轨迹。
9. 查询未命中时，未来可以选择将该查询人物注册为待关注对象，在后续新观测到来时持续匹配。

## 5. 模块划分

### 5.1 目标检测与局部跟踪模块

该模块同时承担图像采集、检测、局部跟踪和完整观测写入职责。不单独设置独立的图像采集模块。

职责：

- 在无人机飞行后启动并持续运行；
- 采集或接收原始 RGB 图像；
- 采集或同步深度图像；
- 记录时间戳；
- 检测画面中的人目标；
- 输出人物在原始图像中的像素框；
- 保存人物裁剪图；
- 为当前无人机视野内的不同人物分配局部 ID；
- 记录无人机编号、相机参数、无人机位姿等辅助元数据；
- 将原始 RGB 图、深度图、人物裁剪图、像素框、局部 ID 和时间戳等信息写入完整数据库；
- 识别“新的局部人物 ID”出现事件，并为摘要数据库提供代表人物图像。

注意：局部 ID 不是全局身份，只能表示当前检测/跟踪模块在局部时空范围内的跟踪结果。

### 5.2 REID 模块

职责：

- 对人物图像计算 embedding；
- 比较查询 embedding 与摘要数据库中历史 embedding 的相似度；
- 使用阈值判断是否命中已有人物；
- 在超过阈值的候选中取 top1 作为识别结果；
- 为历史数据中的新局部人物分配已有或新的全局人物 ID；
- 在查询任务中，如果没有命中摘要数据库，则返回查询失败。

历史入库判定逻辑：

```text
输入：new_local_person_image
输出：global_person_id, match_status, similarity

1. embedding = REID(new_local_person_image)
2. candidates = compare(embedding, summary_database_embeddings)
3. valid_candidates = candidates where similarity >= threshold
4. if valid_candidates is empty:
       create new global_person_id in summary database
       return new global_person_id, "new_person", null
   else:
       top1 = candidate with max similarity
       return top1.global_person_id, "matched", top1.similarity
```

查询判定逻辑：

```text
输入：query_image
输出：query_status, global_person_id, similarity

1. query_embedding = REID(query_image)
2. candidates = compare(query_embedding, summary_database_embeddings)
3. valid_candidates = candidates where similarity >= threshold
4. if valid_candidates is empty:
       return "not_found", null, null
   else:
       top1 = candidate with max similarity
       return "matched", top1.global_person_id, top1.similarity
```

### 5.3 数据存储模块

系统逻辑上维护两类数据库：摘要数据库和完整数据库。

#### 摘要数据库 / 人物库

用于快速 REID 和查询，保存所有入镜过人物的轻量摘要信息。当前设计中，摘要数据库就是人物库，不再单独抽象另一套人物库。

建议字段：

- `global_person_id`：全局人物 ID；
- `source_drone_id`：来源无人机 ID；
- `local_track_id`：局部人物 ID；
- `representative_image_path`：代表人物图像路径；
- `representative_embedding`：代表图像 embedding；
- `first_seen_time`：首次出现时间；
- `last_seen_time`：最近出现时间；
- `match_status`：新增、已匹配等状态；
- `similarity`：与已有全局人物匹配时的相似度；
- `summary_observation_id`：该代表图像对应的完整观测记录 ID。

摘要数据库的核心作用：

- 保存所有历史入镜人物的代表图像和 embedding；
- 维护局部人物 ID 到全局人物 ID 的归并关系；
- 支撑查询图像的 REID 检索；
- 为完整数据库检索提供全局人物 ID。

#### 完整数据库

用于保存完整观测记录，支撑轨迹生成和历史回放。

建议字段：

- `observation_id`：观测记录 ID；
- `global_person_id`：全局人物 ID；
- `source_drone_id`：来源无人机 ID；
- `local_track_id`：局部人物 ID；
- `timestamp`：观测时间；
- `rgb_image_path`：原始 RGB 图像路径；
- `depth_image_path`：深度图像路径；
- `person_crop_path`：人物裁剪图路径；
- `bbox`：人物在原始图像中的像素框；
- `camera_intrinsics`：相机内参；
- `camera_extrinsics`：相机外参；
- `drone_pose`：无人机位姿；
- `estimated_position`：轨迹模块计算出的空间坐标，可后处理写回。

完整数据库的核心作用：

- 保存人物每一次被观测到时的完整信息；
- 支撑按全局人物 ID 查询历史观测；
- 为轨迹生成模块提供深度图、像素框、时间戳和位姿信息。

### 5.4 轨迹生成模块

职责：

- 根据全局人物 ID 获取所有历史观测；
- 从人物像素框和深度图中估计人物深度距离；
- 结合相机内参、相机外参和无人机位姿，将像素坐标转换为空间坐标；
- 对同一全局人物的连续坐标进行时间排序、过滤和连接；
- 输出历史轨迹点序列。

轨迹模块输出建议：

- `global_person_id`；
- `trajectory_points`：按时间排序的坐标点列表；
- `time_range`：轨迹覆盖时间范围；
- `source_observations`：对应的观测记录 ID 列表。

### 5.5 历史轨迹显示模块

首版需要实现人物历史轨迹显示，而不只是预留接口。

职责：

- 接收轨迹生成模块输出的历史轨迹点序列；
- 在二维平面、BEV 图像或其他可视化画布上显示人物历史轨迹；
- 支持展示查询人物的全局人物 ID、轨迹时间范围和轨迹点；
- 在条件允许时，将轨迹坐标映射到 BEV 图像中展示。

首版边界：

- 必须能显示查询人物的历史轨迹；
- 可以先使用简化的二维坐标图或静态 BEV 图；
- 暂不要求实时位置刷新；
- 暂不要求复杂前端交互。

## 6. 多无人机扩展方向

多无人机阶段需要在历史回放闭环基础上扩展为分布式系统，但不要求首版实现。

### 6.1 多无人机基本流程

1. 每架无人机运行目标检测与局部跟踪模块。
2. 每架无人机持续产生完整观测数据，并写入完整数据库。
3. 每架无人机在本地为检测到的人物分配局部 ID。
4. 当本地出现新的局部人物 ID 时，向摘要数据库上传第一张代表人物图像。
5. 摘要数据库计算或接收 embedding，并判断该人物是否为历史全局人物：
   - 若是历史人物，则返回已有全局人物 ID；
   - 若不是历史人物，则新增全局人物 ID。
6. 完整数据库持续保存该全局人物 ID 的完整观测数据。
7. 查询人物到达时，地面站可以把查询任务发送给任意一架无人机。
8. 接收任务的无人机查询摘要数据库，完成 REID 检索。
9. 若命中，则到完整数据库中查询该全局人物 ID 的全部观测。
10. 轨迹模块汇总来自不同无人机的观测，生成跨无人机历史轨迹。

### 6.2 分布式数据库角色

建议逻辑上分为两层：

- **摘要数据库 / 人物库**：保存所有无人机上传的代表人物图像、embedding、全局人物 ID 和出现摘要，用于快速查询和全局 ID 维护。
- **完整观测数据库**：保存所有无人机采集到的完整观测数据，可集中存储，也可由各无人机本地存储、中心按需索引和拉取。

首版历史回放时可以用本地数据库模拟这两类数据库；多无人机阶段再替换为真正的分布式存储和同步机制。

### 6.3 大小无人机与数据库部署

“大小无人机”更多是场景叙事，不作为当前系统设计中的强功能分工。实际设计中，不应假设小无人机只能采集、大无人机才能计算；所有无人机都可以是大无人机，也都可以具备目标检测、局部跟踪和查询摘要数据库的能力。

当前更重要的区别是数据库和服务如何部署：

- 摘要数据库可以是中心化服务，也可以是多无人机共享的分布式服务；
- 完整数据库可以集中存储，也可以分布式存储后由中心索引；
- 查询任务可以由地面站发送给任意无人机；
- 接收查询任务的无人机应能访问摘要数据库，并根据查询结果访问完整数据库或请求其他节点返回完整观测。

## 7. 首版不做或暂不展开的内容

- 不优先实现实时检测入库闭环。
- 不优先实现查询未命中后的实时关注任务。
- 不优先实现完整多无人机分布式同步。
- 不优先处理复杂遮挡、长时间失联、跨摄像头误匹配纠错等高级问题。
- 不在首版强制确定具体数据库产品、消息队列或前端技术栈。
- 不把目标检测局部 ID 直接当作全局身份使用。
- 不单独维护摘要数据库之外的人物库。

## 8. 当前关键设计约束

1. **摘要数据库就是人物库**：每个新入镜人物都会上传一张代表人物图像并计算 embedding，因此摘要数据库包含所有历史入镜人物的轻量身份信息。
2. **查询未命中在首版中表示失败**：当前只做历史回放。如果查询图在摘要数据库中没有超过阈值的候选，则认为该人物没有在历史数据中出现，本次查询失败。
3. **历史入库未命中才新增全局人物 ID**：当历史数据中出现新的局部人物 ID，并且摘要数据库中没有匹配身份时，才新增全局人物 ID。
4. **局部 ID 与全局 ID 必须解耦**：检测/跟踪模块输出的 ID 只表示局部连续性，最终身份必须由摘要数据库中的 REID 归并结果决定。
5. **摘要库服务快速识别，完整库服务轨迹生成**：摘要数据库保存代表图和 embedding；完整数据库保存持续观测细节，避免把所有原始数据都放入快速检索路径。
6. **轨迹生成依赖深度、像素框和位姿信息**：如果缺少相机参数、无人机位姿或深度标定，轨迹只能做到图像坐标或相对坐标，不能可靠输出真实空间坐标。
7. **首版必须显示历史轨迹**：查询命中后，不仅要生成轨迹点序列，还要以二维轨迹图、BEV 图或其他可视化方式显示人物历史轨迹。
8. **先历史回放，后实时扩展**：当前优先验证历史链路正确性，实时查询、实时关注、多无人机分布式同步作为后续目标保留。

## 9. 建议里程碑

### M0：数据与接口梳理

- 明确历史回放数据格式；
- 明确原始 RGB 图、深度图、人物裁剪图、bbox、时间戳和位姿数据字段；
- 明确摘要数据库和完整数据库的最小字段；
- 明确 REID 模块输入输出；
- 明确轨迹生成模块输入输出；
- 明确历史轨迹显示模块输入输出。

### M1：历史数据库构建

- 基于历史回放数据建立完整数据库；
- 从每个新局部人物 ID 中抽取代表人物图像；
- 计算代表人物 embedding；
- 建立摘要数据库；
- 完成局部人物 ID 到全局人物 ID 的归并。

### M2：查询图 REID

- 输入查询人物图像；
- 计算查询 embedding；
- 在摘要数据库中检索相似人物；
- 超过阈值的候选取 top1；
- 未命中时返回查询失败；
- 命中时返回全局人物 ID。

### M3：轨迹生成与历史轨迹显示

- 根据全局人物 ID 查询完整历史观测；
- 根据深度图、像素框和位姿估计人物坐标；
- 生成按时间排序的历史轨迹点序列；
- 显示查询人物的历史轨迹。

### M4：后续实时能力扩展

- 实时目标检测与完整数据库持续写入；
- 新入镜人物实时上传代表图到摘要数据库；
- 查询任务由地面站发送给任意无人机；
- 查询未命中后注册为待关注对象；
- 新观测到来后对待关注对象持续匹配；
- 实时更新人物最新坐标和 BEV 显示。

### M5：多无人机分布式扩展

- 多无人机共享摘要数据库；
- 多无人机完整观测数据集中或分布式存储；
- 任意无人机可接收地面站查询任务；
- 查询节点可访问摘要数据库并拉取完整观测；
- 轨迹模块生成跨无人机历史轨迹。
